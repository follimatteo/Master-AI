Install necessary dependencies

To run a single instance open a shell in this folder and :

python3 src/solve_model.py -f src/Instances/DxD.txt

where D is the dimention of the paper roll

To run all the instances in one 

python3 run_everything.py

This will produce a plot wit the time for each instance

In both models to enable rotation of the pieces add to the call --rotation
